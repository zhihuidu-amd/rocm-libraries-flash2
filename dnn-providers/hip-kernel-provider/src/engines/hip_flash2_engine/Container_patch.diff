// Container.cpp patch for ROCm/rocm-libraries
// dnn-providers/hip-kernel-provider/src/core/Container.cpp
//
// Add these lines to register HipFlash2Engine alongside the existing engines.
// This is the ONLY file that needs to change in the hip-kernel-provider codebase.
//
// === DIFF to apply ===
//
// +#ifdef HIPDNN_ENGINE_HIP_FLASH2
// +#include "engines/hip_flash2_engine/HipFlash2FwdPlanBuilder.hpp"
// +#endif
//
// Inside getEngineDefinitions(), after the ASM_SDPA block:
//
// +#ifdef HIPDNN_ENGINE_HIP_FLASH2
// +    // HIP_FLASH2_ENGINE: FP16 Flash-Attention 2 (V7 kernel: rocWMMA + causal skip)
// +    // Handles FP16 inputs where ASM SDPA engine only handles BF16.
// +    // Achieves 78.98 TFLOPS on MI325X, 71.27 TFLOPS on MI300X at seq=4096 causal.
// +    {HIP_FLASH2_ENGINE_ID,
// +     [](const compilation::IKernelCompiler& kernelCompiler,
// +        const device::IDevicePropertyProvider& devicePropertyProvider)
// +         -> std::unique_ptr<hipdnn_plugin_sdk::IEngine<Handle, Settings, Context>> {
// +         auto engine = std::make_unique<hip_flash2_engine::HipFlash2Engine>();
// +         engine->addPlanBuilder(
// +             std::make_unique<hip_flash2_engine::HipFlash2FwdPlanBuilder>(
// +                 kernelCompiler, devicePropertyProvider));
// +         return engine;
// +     }},
// +#endif
//
// === CMakeLists.txt addition ===
// In dnn-providers/hip-kernel-provider/CMakeLists.txt, add:
//
// option(ENABLE_HIP_FLASH2_ENGINE "Enable HIP Flash-Attention 2 SDPA engine" ON)
// if(ENABLE_HIP_FLASH2_ENGINE)
//     target_compile_definitions(hip_kernel_provider PRIVATE HIPDNN_ENGINE_HIP_FLASH2)
//     target_sources(hip_kernel_provider PRIVATE
//         src/engines/hip_flash2_engine/HipFlash2Engine.cpp
//         src/engines/hip_flash2_engine/HipFlash2FwdPlanBuilder.cpp
//         src/engines/hip_flash2_engine/HipFlash2FwdPlan.hip)
// endif()
//
// The engine ID must also be registered in:
//   hipdnn_data_sdk/utilities/EngineNames.hpp
//   (add: constexpr int64_t HIP_FLASH2_ENGINE_ID = 0x4841544E32ULL;)
//
// See full implementation in AMD-AIOSS/FlashInfer-hipDNN/src/hipdnn_engine/

// This file is documentation only — no compilable code here.
// The actual implementation is in the .hip and .hpp/.cpp files alongside this.
